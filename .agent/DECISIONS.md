# Decisions

> CANDIDATE = auto-extracted (unverified). A human flips to active.

## 2026-08-08 — CANDIDATE (auto-extracted · session b75a191b)
- — the papers carry internal titles "CLAT 2027 MOCK TEST – N", so "MOCK TEST – 1/2" are Mocks 1 and 2. Let me confirm all ten.

## 2026-08-20 — CANDIDATE (auto-extracted · session 8fa1fbf0)
- the labels are now clipped visually instead of removed, so they stay in the accessibility tree at every width. The badge count too. Ten lines of CSS.
